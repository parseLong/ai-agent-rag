---
title: 十年老技术开发的 AI Agent 探索之路
description: 从手动管 4-6 个终端到 24h 无人值守 Agent 系统——人在瓶颈、80%需求不需要AI、Vibe Coding翻车、SDD流程、Agent自举、脚手架>模型、从Task-Driven到Goal-Driven的完整实践之路
tags:
  - 概念卡片
  - 永久笔记
  - 实践
  - 智能体
  - SDD
  - Harness
  - Goal-Driven
author: zhiyuanfu
source: 腾讯技术工程
url: https://mp.weixin.qq.com/s/r__2l_u6oXwzWHyD3wu_ZQ
date: 2026-06-15
---

# 十年老技术开发的 AI Agent 探索之路

> 曾经前端被戏称为"娱乐圈"——工具、框架层出不穷。现在 AI 把这个周期压缩到了以月计。这篇文章，就是一个在"AI 娱乐圈"摸爬滚打的老开发，试图从月抛式的焦虑中找到不会过期的东西。

---

## 核心脉络

![[笔记同步助手/images/b505964a0c4b20cd77fb69439e292285_MD5.gif]]

整篇文章是一条递进式的实践之路：

| 阶段 | 核心认知 | 关键转折 |
|------|----------|----------|
| 起点 | **人是瓶颈**——4-6 个终端并发上限 | 从"让AI更聪明"转向"让系统不再依赖人的实时在场" |
| 简化 | **80%的AI需求不需要AI** | 代码优先于 Prompt，10行Bash搞定就别折腾AI |
| 翻车 | **[[Vibe Coding]] 是先易后难** | Day 15 整整一天"设计与实现对齐"——建立了全部基础 |
| 造系统 | **[[SDD]] 流程 + 文件轮询调度** | 24h 无人值守，20-30 个并发任务稳定执行 |
| 自举 | **Agent 自己修了自己的 bug** | 不是碰运气，而是 [[SDD]] + constitution.md 的系统性结果 |
| 治理 | **[[脚手架优先原则]] > 模型** | 脚手架升级：成本 +50%，效果 +200%；模型升级：成本 +300%，效果 +20% |
| 协议 | **行业正在收敛到协议 + runtime + control plane** | [[MCP]] / [[A2A]] / Responses API 正在形成共识性基础设施 |
| 跃迁 | **[[Task-Driven vs Goal-Driven]]** | 从"人派活"到"系统围绕目标自主推进" |

---

## 第一章：人是瓶颈

同时开 5 个 AI 终端的真实体验：codex 跑了五分钟没动静、gemini 改完接口忘了哪个分支、claude 写的文档引用旧接口名、Cursor 弹了确认框白等十分钟。**4-6 个并发是上限，人脑的 context switch 开始崩溃。**

| 限制 | 具体表现 | 后果 |
|------|----------|------|
| 吞吐有限 | 一天能管 4-6 个 Agent 窗口 | 任务量有硬上限 |
| 状态不稳定 | 上下文丢失、判断漂移、质量波动 | 上午管5个，下午犯困管不了3个 |
| 难以规模化 | 做成一次不难，稳定重复难 | 今天的成功经验明天就忘了 |

> **人是瓶颈。但解决瓶颈的方式不是让 AI 替代人，而是让系统不再依赖人的实时在场。**

---

## 第二章：80% 的 AI 需求不需要 AI

第一件事不是调模型、搞 RAG，而是写 Bash 脚本自动化日常工作流。结果发现——**80%的"AI 需求"根本不需要 AI。**

自动拉取代码跑测试？Bash。定时检查服务健康状态？`cron + curl`。把 JSON 日志格式化成报表？`jq + awk`。文件变更触发构建？`inotifywait + shell`。

> **代码优先于 Prompt。能用 10 行 Bash 解决的，别折腾 AI。**

### [[AI 需求决策层级]]

![[笔记同步助手/images/f6167a893a63c55fb38ab1ca2601dee2_MD5.png]]

**目标 → 代码 → CLI → Prompt → Agent。** 每往上一层，不确定性增加一个量级，成本也增加一个量级。原则：**能在下层解决的，绝不上推。**

| 层级 | 适用场景 | 示例 | 不确定性 |
|------|----------|------|----------|
| 目标层 | 想清楚到底要解决什么 | 想清楚后发现不需要写代码 | 最低 |
| 代码层 | 确定性逻辑 | if/else、正则、模板引擎 | 低 |
| CLI 层 | 组合现有工具 | `grep + jq + curl`串流程 | 中低 |
| Prompt 层 | 需要语义理解和判断 | 需求翻译、文案生成 | 中高 |
| Agent 层 | 多步推理、动态决策、循环执行 | 自动修 bug、端到端流程 | 最高 |

---

## 第三章：Vibe Coding 翻车

详见 → [[Vibe Coding]]

24h 打工人项目初期的真实时间线：

| 天数 | 状态 | 具体表现 |
|------|------|----------|
| Day 1-3 | ✨ 惊喜 | 几句话出完整页面，成就感爆棚 |
| Day 7 | ⚠️ 开始乱了 | 打地鼠——修了这个 bug 冒出那个 |
| Day 14 | 🔥 灾难 | 大量过度设计、三层抽象解决一个函数搞定的问题 |
| Day 15 | 🔧 对齐日 | 整整一天"设计与实现对齐"，比前两周加起来都累，**但价值也比前两周加起来都大** |

> **[[Vibe Coding]] 是先易后难。[[SDD]] 是先难后易。大道如夷，而民好径。**

Day 15 建立了让系统后续能自动运转的全部基础——设计文档、架构约束、[[SDD]] 流程。没有这一天，就没有后面的 24h 打工人。

---

## 第四章：24h 打工人——第一个真正的系统

翻车之后，先设计再动手。

### 为什么选 CLI 而不是 API

在当时的阶段里，codex、gemini-cli、claude-code 这些 CLI 工具已经内置了读文件、改代码、跑命令的能力——它们本身就是完整的 Agent。我要做的是**管理 Agent 的调度层**，而非重新造 Agent。

> CLI 是最低成本、最容易 debug、最利于 AI 直接读取和修复的方案。工具是手段，不是信仰。

### 核心架构：文件 + 轮询

```python
class AgentScheduler:
    def __init__(self):
        self.queue = FileQueue("storage/feedbacks/")
        self.tools = ToolProber(["codex", "gemini-cli", "claude"])
        self.state = JsonStateManager("storage/state/")

    def run(self):
        while True:
            task = self.queue.poll()
            if not task:
                time.sleep(10); continue
            tool = self.tools.get_available()  # 自动选可用工具
            try:
                result = tool.execute(task.prompt, task.workspace)
                self.state.update(task.id, status="done", output=result)
            except QuotaExhausted:
                self.tools.cooldown(tool, duration=300)
                self.queue.requeue(task)  # 重新入队，换工具执行
```

调度层做四件事：

1. **接收任务**：用户反馈写入文件队列
2. **分发执行**：轮询队列，调用 CLI 执行
3. **状态管理**：记录每一步的输入输出，持久化到文件
4. **失败切换**：某个 CLI 配额用完，自动换下一个

### [[SDD]]：让 AI 的每一步都有据可查

每个需求处理完留下的一组文档：

```
storage/feedbacks/2026-01-15/20260115-143021-a1b2c3/
├── sdd/
│   ├── spec.md      # 规格说明：目标、验收标准
│   ├── plan.md      # 技术方案：涉及文件、实现步骤
│   └── tasks.md     # 任务清单：每个任务的描述和状态
├── tasks.json       # 任务执行状态（供程序读取）
└── debug/
    ├── prompts/     # 每一步的 prompt
    └── agent.log    # 执行日志
```

`spec.md` 把"分页有问题"变成"切换页码后列表数据未刷新，原因是查询参数未传递 page 参数"。`plan.md` 把问题变成方案。`tasks.md` 把方案拆成可执行的步骤。

没有这些记录，系统就只能不断"重来一次"；有了这些记录，系统才可能真正"学会下一次做得更好"。

> **留痕不是为了 debug，而是为了进化。**

### 智能并发策略

| 策略 | 具体做法 | 理由 |
|------|----------|------|
| 组间并发 | 前端任务和后端任务同时跑 | 代码在不同目录，不会冲突 |
| 组内串行 | 同一个前端项目的任务排队执行 | 可能修改同一文件，避免冲突 |
| 失败隔离 | 单个任务失败不影响其他组 | 故障不扩散 |

> 并行的本质不是"同时做很多事"，而是"让每件事都不需要等别人"。

### 工具失败自动切换

```
正常流程：task → codex（可用）→ 执行成功 → 完成
失败切换：task → codex（配额耗尽）→ gemini-cli → 执行成功 → 完成
全部不可用：task → 等待 → 5 分钟后自动探活 → 恢复后继续
```

从 4 个终端的手忙脚乱，到 **20-30 个并发任务**的稳定执行。

---

## 第五章：Agent 自己修了自己的 bug

详见 → [[Agent 自举]]

通过 24h 打工人自己的反馈系统提交 bug → 系统自动触发 [[SDD]] 流程 → AI 生成方案并拆解任务 → 确认澄清结果 → 执行修复 → 企微通知完成。

**它自己修复了自己的 bug。** 整个过程只做了两件事：提交反馈、确认澄清。

自举的前提是 Day 15 建立的三样东西：

1. **清晰的设计文档** —— AI 知道每个模块该做什么、不该做什么
2. **[[SDD]] 流程** —— spec → plan → tasks 的标准路径
3. **constitution.md** —— 架构约束文件，定义代码组织规范、命名规则、模块边界

> **捷径的尽头是弯路，大道的尽头是自由。**

---

## 第六章：从 demo 到系统——门槛不是模型，是治理

### "demo 跑偏时的 4 个不知道"

1. 是目标有歧义，还是分解有问题？
2. 是工具挂了，还是权限不够？
3. 是 Prompt 不稳定，还是系统边界不清？
4. 是这次偶然成功，还是可以稳定复现？

### [[Agent 可观测性]]：6 个必看维度

```
observability_dimensions:
  1_goal:      "当前目标是什么"
  2_step:      "正在执行哪一步"
  3_tool:      "用了什么工具"
  4_failure:   "为什么失败"
  5_recovery:  "是否触发了重试 / 回退 / 切换"
  6_cost:      "本轮 token / 时间 / 成本消耗了多少"
```

### [[Agent 评估闭环]]：4 个持续校准问题

| 评估问题 | 为什么重要 |
|----------|------------|
| 需求澄清是否稳定？ | 不稳定意味着下游全部白跑 |
| 任务拆解是否越来越合理？ | 拆解质量决定执行效率 |
| 某个 Skill 是否真的提高了成功率？ | 避免"加了等于没加" |
| 失败切换有没有制造新错误？ | 避免"修了一个坑，挖了两个坑" |

> 对 Agent 来说，**评估不是验收动作，而是日常运行信号。**

### Control Plane：权限、边界、审计

- 谁可以调用哪些工具？
- 哪些动作默认允许，哪些必须人工确认？
- 哪些路径只能读，哪些能写？
- 哪些任务失败后必须停下来？

> 从 demo 到系统，中间隔着的不是更多 Prompt，而是 control plane。

### [[脚手架优先原则]] > 模型

```
投入回报对比（个人经验估算）：

模型升级：成本 +300%，效果 +20%
脚手架升级：成本 +50%，效果 +200%

→ 优先投资脚手架，而不是追最新最贵的模型
```

24h 打工人用的不是最贵的模型——codex 配额用完切 gemini，gemini 挂了切 claude。但有 [[SDD]] 流程 + 调度层 + 失败切换 + constitution.md，效果远好于一次性顶级模型。

> **垃圾的思考乘以强大的模型，等于精美的垃圾。**

### Agent 系统的 5 层地基

1. **目标表达**：到底想完成什么
2. **能力单元**：有哪些 Skill / 工具 / 工作流
3. **运行时状态**：当前正在做什么
4. **治理边界**：允许做什么，不允许做什么
5. **评估反馈**：哪些行为值得固化，哪些必须修正

> 少任何一层，系统都可能看起来能跑，但跑不稳。

---

## 第七章：协议层正在成形

详见 → [[Agent 协议层收敛]]

| 时间 | 事件 | 意义 |
|------|------|------|
| 2025-03-11 | OpenAI 发布 Agents 新基座 | Responses API + Tools + SDK + Tracing，runtime 开始收敛 |
| 2025-04-09 | Google 发布 [[A2A]] | Agent 间协作有了协议化趋势 |
| 2025-06-23 | A2A 捐给 Linux Foundation | 从企业项目变行业标准 |
| 2026-02-13 | GitHub 发布 Agentic Workflows 技术预览 | Agent 进入 CI/PR/Issue 主流程 |
| 2026-03-16 | Microsoft Foundry Agent Service GA | Responses API runtime + 全链路 tracing + 企业级 eval |
| 2026-04-07 | GitHub Dependabot → AI agent remediation | 安全告警自动修复成为现实 |

> **Agent 开发正在从"框架之争"，转向"协议 + runtime + control plane 之争"。**

未来做 Agent，越来越像搭操作系统，不只是写 prompt。

---

## 第八章：从 Task-Driven 到 Goal-Driven

详见 → [[Task-Driven vs Goal-Driven]]

### 核心转变

![[笔记同步助手/images/1b26d395f162a1995114106671694478_MD5.png]]

| 维度 | Task-Driven | Goal-Driven |
|------|-------------|-------------|
| 人的角色 | 项目经理 + 执行监督 | 目标设定者 / 审核者 |
| Agent 的角色 | 执行器 | 自主推进者 |
| 决策中心 | 在人脑子里 | 在目标 + 边界 + 系统状态里 |
| 主要成本 | 人持续编排 | 前期建模和约束设计 |
| 适用场景 | 简单、一次性任务 | 期、复杂、持续推进任务 |

> **Task-Driven 解决执行问题，Goal-Driven 解决迭代问题。**

### Goal-Driven 的 5 个前提

1. **目标必须清晰** —— 可推进、可判断的目标表达
2. **边界必须清晰** —— 哪些能做、不能做，资源上限
3. **状态必须可见** —— 当前做到哪一步，卡在哪
4. **过程必须留痕** —— 否则无法知道成功/失败的原因
5. **权限必须可控** —— 能调用哪些工具，能写到哪里，谁来兜底

> Goal-Driven 不是更放权，而是更强约束下的有限自治。

### 共享状态：STATE.yaml

```yaml
# STATE.yaml — 共享任务面板
goal: "优化搜索模块响应速度，P95 从 800ms 降到 200ms"
owner: "joefu"
deadline: "2026-05-01"
constraints:
  - "不修改已有 API 契约"
  - "每日 API 成本不超过 $5"
  - "所有架构变更需记录在 changelog.md"

agents:
  profiler:
    status: "completed"
    output: "analysis/search-perf-baseline.json"
    summary: "瓶颈定位在 DB 全表扫描和缺少缓存层"
  backend_dev:
    status: "in_progress"
    current_step: "为热点查询添加 Redis 缓存"
  test_runner:
    status: "pending"
    depends_on: ["backend_dev"]
    description: "回归测试 + 性能压测验证"

next_review: "2026-04-15"
```

每个 Agent 自己读取状态、写回进度，主会话只负责高层目标和验收。

### 6 步落地路径

| 步骤 | 做什么 | 核心产出 |
|------|----------|----------|
| 第一步 | 写清楚 spec | 要做什么、不做什么、怎么算完成 |
| 第二步 | 执行过程留痕 | Prompt/状态/输出/错误全记录 |
| 第三步 | 补 observability 和 eval | 知道为什么成功、为什么失败 |
| 第四步 | 高频动作沉淀为 Skill | 模板 + 规则 + 代码 |
| 第五步 | 引入调度和并发 | 调度层 + 轮询 + 失败切换 |
| 第六步 | 最后才尝试 Goal-Driven | 目标表达 + 治理边界 + 共享状态 |

> **先让一次执行可复盘，再让它可重复，再让它可规模化，最后让它可有限自主。**

---

## 结语

![[笔记同步助手/images/b43363f42804d0c4cda0d648fbc124d8_MD5.gif]]

瓶颈不是人的能力不够，而是人的注意力有限。解决方案不是"让 AI 替代人"，而是"让系统不再依赖人的实时在场"。

24h 打工人的 [[SDD]] 是系统地基：
- `spec.md` 把模糊的需求变成明确的目标
- `plan.md` 把目标变成技术方案
- `tasks.md` 把方案变成可执行的步骤
- `constitution.md` 把经验变成可复用的约束
- `eval / trace / policy` 把系统变成可观测、可治理、可持续迭代的能力体

> **真正的跃迁，不是让 AI 多做几个步骤，而是让人退出微观调度。**
> **增强自我，而非取代自我。共勉。**

![[笔记同步助手/images/5053bdadaf077578398a0f9307eab152_MD5.png]]

---

## 关联知识

- [[SDD]] — Spec-Driven Development，让 AI 的每一步有据可查
- [[Vibe Coding]] — 先易后难的翻车教训
- [[脚手架优先原则]] — 手架 > 模型，投资治理而非追模型
- [[Task-Driven vs Goal-Driven]] — 从执行到迭代的认知跃迁
- [[Agent 自举]] — Agent 在框架内自己修自己的 bug
- [[Agent 可观测性]] — 6 个维度让系统从黑盒到白盒
- [[Agent 评估闭环]] — 评估是日常运行信号而非验收动作
- [[AI 需求决策层级]] — 目标→代码→CLI→Prompt→Agent 的决策阶梯
- [[Agent 协议层收敛]] — 从框架之争到协议+runtime+control plane
- [[Harness Engineering]] — 驭驭工程，Agent 外围的执行与控制层
- [[MCP]] — 工具接入标准化协议
- [[A2A]] — Agent 间协作协议
- [[Context Engineering]] — 从提示工程到上下文工程的范式演进
- [[工程设计与安全]] — 构建者信条与安全防护模式
- [[5条驭驭工程原则]] — 跨厂商的驭驭工程核心设计约束
- [[智能体评估]] — 综合评估指南

## 参考来源

- [十年老技术开发的 AI Agent 探索之路](https://mp.weixin.qq.com/s/r__2l_u6oXwzWHyD3wu_ZQ) — zhiyuanfu，腾讯技术工程，2026-05-07
